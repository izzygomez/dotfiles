- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.emacs: emacs
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.tmux-powerlinerc: tmux-powerlinerc
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
